```
[3장 실습]

/******************************************************************************
 * 코드 추가 1
 ******************************************************************************/
// 1) 메뉴 [File]->[New]->[C/C++ Project] 한 후 새로운 프로젝트 CH3을 만들어라.
// 2) 메뉴 [File]->[New]->[Source Folder] 한 후 폴더 이름으로 src를 지정하여 생성하라.
// 3) 메뉴 [File]->[New]->[Source File]   한 후 폴더 이름으로는 위에서 지정한 CH3/src로 설정하고
//    파일 이름으로 ch3.cpp를 지정하여 소스파일을 만들어라.
// 4) 아래 코드를 소스파일로 복사하라.
//-----------------------------------------------------------------------------

===============================================================================
== 프로그램 복사
=============================================================================== 

// 적절한 head 파일 include할 것
 
class Person
{
    string name;
    int    id;
    double weight;
    bool   married;

public:
    void setName(string _name);

    void println();
};

void Person::setName(string _name)   {
    name = _name;
}

void Person::println()   {
    // ch2.cpp의 화면출력 코드를 참조할 것
}

void set(string name, int id, double weight, bool married) {
    Person p1;
    p1.setName(name);
    p1.setId(id);
    p1.setWeight(weight);
    p1.setMarried(married);
    p1.println();
}

void creator() {
}

int main() {
    cin >> boolalpha;
    cout << boolalpha;
    // married의 값을 1, 0이 아닌 true, false로 입력 받고 출력하기 위함 

    string name = "p1";
    int id = 1001;
    double weight = 72.5;
    bool married = true;

    set(name, id, weight, married);
}


===============================================================================
== 문제 1
=============================================================================== 
// 아래 [실행결과 1]은 void set(string name, int id, double weight, bool married) 내의
// p1.println();에 의해 출력된 결과이다.
// 각 멤버의 값을 출력하는 이 함수를 먼저 구현하라.
// 그리고 멤버함수 Person::setName(string _name)의 구현 함수를 참조하여
// setId(int id), setWeight(double weight), setMarried(bool married)
// 멤버 함수들을 구현하라.

===============================================================================
== 실행결과 1
=============================================================================== 
name:p1, id:1001, weight:72.5, married:true

===============================================================================
== 문제 2
=============================================================================== 
// 아래 [실행결과 2]처럼 출력될 수 있도록 생성자 Person()을 구현하라.
// 이 생성자는 각 멤버 값을 초기화한 후 아래처럼 출력한다.
Person::Person():name:no-name, id:0, weight:0, married:false
// 이를 위해 먼저 각 멤버 변수의 값을 위 출력 결과 값으로 초기화한다.
// 그런 후 "Person::Person():"을 출력한다.
// 마지막으로 println(); 멤버함수를 호출한다. (각 멤버 값 출력)

===============================================================================
== 실행결과 2
=============================================================================== 
Person::Person():name:no-name, id:0, weight:0, married:false
name:p1, id:1001, weight:72.5, married:true

===============================================================================
== 문제 3
=============================================================================== 
// 아래 [실행결과 3]처럼 출력될 수 있도록 소멸자 ~Person()을 구현하라.
// 이 소멸자는 아래 메시지만 출력하면 된다.
Person::~Person():name:p1, id:1001, weight:72.5, married:true
// 이를 위해 먼저 "Person::~Person():"을 출력한다.
// 그런 후 println(); 멤버함수를 호출한다. (각 멤버 값 출력)

===============================================================================
== 실행결과 3
=============================================================================== 
Person::Person():name:no-name, id:0, weight:0, married:false
name:p1, id:1001, weight:72.5, married:true
Person::~Person():name:p1, id:1001, weight:72.5, married:true

===============================================================================
== 문제 4
=============================================================================== 
// 기존 creator() 함수에 다음 내용을 추가하라.
//-----------------------------------------------------------------------------
void creator() {
    cout << "creator() begin" << endl;
    Person p2("p2", 1002, 60.5, false);
    cout << "creator() return" << endl;
}

//-----------------------------------------------------------------------------
// 기존 main() 함수 끝에 다음 내용을 추가하라.
//-----------------------------------------------------------------------------
    cout << endl;
    creator();

//-----------------------------------------------------------------------------
// 위 Person p2("p2", 1002, 60.5, false);에 의해 새로운 객체 p2가 초기화될 수 있도록
// 적절한 생성자를 구현하라. class Person에 아래 생성자를 선언하라.
//-----------------------------------------------------------------------------
    Person(string _name, int _id, double _weight, bool _married);

//-----------------------------------------------------------------------------
// 구현시 이 생성자는 매개변수로 넘어 온 값으로 각 멤버를 초기화한 후 아래처럼 출력한다.
Person::Person(...):name:p2, id:1002, weight:60.5, married:false
// 이를 위해 각 멤버 변수 값을 상응하는 매개변수 값으로 초기화한다.
// 그런 후 "Person::Person(...):"을 출력한 후 println(); 멤버함수를 호출한다.

===============================================================================
== 실행결과 4
=============================================================================== 
Person::Person():name:no-name, id:0, weight:0, married:false
name:p1, id:1001, weight:72.5, married:true
Person::~Person():name:p1, id:1001, weight:72.5, married:true

creator() begin
Person::Person(...):name:p2, id:1002, weight:60.5, married:false
creator() return
Person::~Person():name:p2, id:1002, weight:60.5, married:false

===============================================================================
== 문제 5
=============================================================================== 
// 기존 main() 함수 끝에 다음 내용을 추가하라.
//-----------------------------------------------------------------------------
    cout << endl;

    cout << "name id weight married? ";
    cin >> name >> id >> weight >> married;
    Person p3(name, id, weight, married);

    cout << "output name:" << p3.getName() << ", id:" << p3.getId() << ", weight:" <<
    	    p3.getWeight() << ", married:" << p3.getMarried() << endl;

//-----------------------------------------------------------------------------
// 기존 class Person에 아래 멤버 함수 선언을 추가하라.
//-----------------------------------------------------------------------------
    string getName();

//-----------------------------------------------------------------------------
// 그리고 위 멤버 함수를 구현한 아래 함수를 class Person의 외부 적절한 곳에 배치하라.
//-----------------------------------------------------------------------------
string Person::getName() {
    return name;
}

//-----------------------------------------------------------------------------
// 위 멤버함수 선언 및 구현을 참고하여 나머지 getId(), getWeight(), getMarried()
// 멤버함수들을 구현하라. 실행결과는 아래와 같다.

===============================================================================
== 실행결과 5
=============================================================================== 
Person::Person():name:no-name, id:0, weight:0, married:false
name:p1, id:1001, weight:72.5, married:true
Person::~Person():name:p1, id:1001, weight:72.5, married:true

creator() begin
Person::Person(...):name:p2, id:1002, weight:60.5, married:false
creator() return
Person::~Person():name:p2, id:1002, weight:60.5, married:false

name id weight married? Hong 1003 73.4 true
Person::Person(...):name:Hong, id:1003, weight:73.4, married:true
output name:Hong, id:1003, weight:73.4, married:true
Person::~Person():name:Hong, id:1003, weight:73.4, married:true
```